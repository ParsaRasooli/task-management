import { HoverSpanDirective } from './hover-span.directive';

describe('HoverSpanDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverSpanDirective();
    expect(directive).toBeTruthy();
  });
});
